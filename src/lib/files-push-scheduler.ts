// Sheets push scheduler — no-op stub (Sheet sync not enabled in this app).
// API surface preserved so call sites compile.

export function scheduleStoragePush(): void {
  // no-op
}

export function scheduleFilesPush(): void {
  // no-op
}
